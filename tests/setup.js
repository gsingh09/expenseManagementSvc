// Setup file for Jest tests
// Can be used to initialize test database, mock modules, etc.

beforeAll(async () => {
  // Setup code if needed
});

afterAll(async () => {
  // Cleanup code if needed
});
